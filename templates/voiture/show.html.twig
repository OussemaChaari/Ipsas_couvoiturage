{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>Modele</th>
                <td>{{ voiture.modele }}</td>
            </tr>
            <tr>
                <th>Clim</th>
                <td>{{ voiture.clim ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Fumeur</th>
                <td>{{ voiture.fumeur ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Bagage</th>
                <td>{{ voiture.bagage ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ voiture.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('voiture_index') }}">back to list</a>

    <a href="{{ path('voiture_edit', {'id': voiture.id}) }}">edit</a>

    {{ include('voiture/_delete_form.html.twig') }}
{% endblock %}
